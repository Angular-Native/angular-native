---
title: angular-native
description: Angular apps that render real native views on eight platforms. No WebView anywhere.
template: splash
eyebrow: 25 primitives · 8 platforms · one Rust core
command: |
  npx @angular/cli@latest new my-app
  cd my-app && an init
announce: Plugins now load on macOS and the Apple Watch
announceLink: /extending/plugins-on-the-mac-and-the-watch/
hero:
  title: |
    Real native views.<br /><em>No WebView anywhere.</em>
  tagline: |
    Write Angular. Get a UIView, an android.view.View, an NSView or a SwiftUI
    view — the real control, from the real system, on eight platforms.
  actions:
    - text: Quick start
      link: /guide/quick-start/
      icon: right-arrow
      variant: primary
    - text: Why not a WebView
      link: /guide/how-it-works/
      icon: open-book
      variant: minimal
---

import PlatformGrid from '../../components/PlatformGrid.astro'
import Signature from '../../components/Signature.astro'
import Showcase from '../../components/Showcase.astro'
import Bento from '../../components/Bento.astro'
import Closing from '../../components/Closing.astro'
import Section from '../../components/Section.astro'
import Commands from '../../components/Commands.astro'

<Signature
  eyebrow="The whole argument"
  title="One template. Whatever that platform actually ships."
  body="Nothing here is drawn to resemble a control. The template does not change between one platform and the next — what changes is the class the framework mounts, and where a platform has no equivalent, nothing is mounted and the host says why."
  becomesLabel="an-switch becomes"
  code={`<an-view [style.gap]="'16'">
  <an-text [fontSize]="28">Alerts</an-text>
  <an-switch
    [on]="alerts()"
    (onChange)="alerts.set($event)" />
</an-view>`}
  answers={[
    {
      platform: 'iOS · iPadOS',
      becomes: 'UISwitch',
      note: "The system's own switch: its animation, its haptic, its accessibility trait, and whatever it looks like in the next release of iOS."
    },
    {
      platform: 'Android',
      becomes: 'MaterialSwitch',
      note: 'Material 3, resolved without Gradle. It follows the device theme, including the dynamic colour a user picked from their wallpaper.'
    },
    {
      platform: 'macOS',
      becomes: 'NSSwitch',
      note: 'AppKit, in a window that resizes — with a pointer that has a shape and a hover state, both of which a finger does not have.'
    },
    {
      platform: 'watchOS',
      becomes: 'Toggle',
      note: 'The one host that is not a view hierarchy: the tree is mirrored into a model SwiftUI redraws.'
    },
    {
      platform: 'visionOS',
      becomes: 'UISwitch',
      note: 'The same host as the phone, in a windowed scene. A window in space is not a screen, but it is still UIKit.'
    },
    {
      platform: 'Wear OS',
      becomes: 'MaterialSwitch',
      note: "The phone's host on a round screen, where the safe area is how the bezel's margin arrives."
    },
    {
      platform: 'tvOS',
      becomes: 'nothing is mounted',
      absent: true,
      note: 'UISwitch is not in the tvOS SDK — a television is driven with a remote. The layout keeps the gap it had measured and the host says so once, in the log.'
    }
  ]}
/>

<Bento
  panels={[
    {
      wide: true,
      title: 'Real controls, not lookalikes',
      body: 'Nothing is drawn to resemble a control. The switch in your template <strong>is</strong> the system\u2019s switch, so it looks and behaves however that control does in that version of the OS, and it ages with the platform instead of with this repository.',
      rows: [
        { label: 'iOS · iPadOS', value: 'UISwitch' },
        { label: 'Android', value: 'MaterialSwitch' },
        { label: 'macOS', value: 'NSSwitch' },
        { label: 'watchOS', value: 'Toggle' },
        { label: 'visionOS', value: 'UISwitch' },
        { label: 'tvOS', value: 'not in the SDK' }
      ]
    },
    {
      title: 'The layout is not the platform\u2019s',
      body: 'UIKit, AppKit and Android lay out differently, and SwiftUI does not let you lay out at all \u2014 so layout leaves the platform entirely. Flexbox runs once, in a Rust core over taffy, and every host is handed absolute frames in logical points. The same template lays out identically on a phone, a TV and a watch.'
    },
    {
      title: 'Angular as it is',
      body: 'Signals, <code>@if</code>, <code>@for</code>, the router with parameters, AOT templates, dependency injection. Zoneless, and not optionally: without zone.js there are no timers or XHR to patch inside the engine, which is most of what makes this painful elsewhere. No dialect, no subset.'
    },
    {
      title: 'A gap is never silent',
      body: 'A prop no host reads, a style nobody recognises, an event a platform cannot deliver, a plugin missing a platform: each one warns, or stops the build. A primitive that is unsupported somewhere has to say so in writing, and scripts enforce it.',
      output: '[angular-native] (pinch) does not arrive on watchOS:\n  MagnifyGesture is @available(watchOS, unavailable),\n  and two fingers do not fit on a 40 mm screen'
    }
  ]}
/>

<Showcase
  eyebrow="Recorded on a simulator, not mocked up"
  title="Two apps out of examples/, running."
  body="Both of these are ordinary Angular components — signals, @for, @if — and every control in them is the platform's own. The recordings are the iOS simulator, driven by hand: the search field is a UISearchBar, the size picker is a UISegmentedControl, the quantity is a UIStepper, and the stars are SF Symbols."
  demos={[
    {
      title: 'A gallery that reflows',
      body: 'Two columns dealt out by running height, a system search field, a category strip and pull-to-refresh. There is no grid in the layout engine and none is needed: a waterfall is two flex columns, resolved once in Rust.',
      video: '/video/feed.mp4',
      poster: '/img/shots/feed-poster.jpg',
      href: '/guide/lists/',
      linkText: 'How long lists work',
      uses: ['an-search-bar', 'an-segmented-control', 'an-image', 'an-scroll-view']
    },
    {
      title: 'A product page you can swipe',
      body: 'The carousel is the one thing here that is not a system control, because no platform ships one — so it is a pan gesture moving a transform, and letting go hands the rest to the platform\u2019s own drawing thread. The stepper and the switch beside it are UIKit\u2019s.',
      video: '/video/storefront.mp4',
      poster: '/img/shots/storefront-poster.jpg',
      href: '/guide/gestures-and-animation/',
      linkText: 'Gestures and animation',
      uses: ['an-stepper', 'an-switch', 'an-segmented-control', '(pan)', '[animate]']
    }
  ]}
/>

<Section
  eyebrow="Where it runs"
  title="Eight platforms, one bundle"
  body="The same bundle on all of them. What differs is not the code — it is which controls that platform ships, and every gap is listed with the reason for it."
/>

<PlatformGrid
  rendersLabel="Renders with"
  platforms={[
    { name: 'iOS · iPadOS', renders: 'UIKit', state: 'All 25 primitives', level: 'full' },
    {
      name: 'Android',
      renders: 'android.view + Material 3',
      state: 'All 25 primitives',
      level: 'full'
    },
    { name: 'macOS', renders: 'AppKit', state: 'All 25 primitives', level: 'full' },
    {
      name: 'tvOS',
      renders: 'UIKit + focus engine',
      state: '20 of 25 — a TV has no switch or slider',
      level: 'partial'
    },
    {
      name: 'visionOS',
      renders: 'UIKit in a windowed scene',
      state: 'All 25 primitives',
      level: 'full'
    },
    { name: 'watchOS', renders: 'SwiftUI', state: '17 of 25', level: 'partial' },
    { name: 'Wear OS', renders: 'android.view', state: '18 of 25', level: 'partial' },
    { name: 'HarmonyOS · Windows', renders: '—', state: 'Not started', level: 'none' }
  ]}
/>

<Commands
  eyebrow="In one minute"
  title="Four commands on a project you already had"
  body="An Angular CLI project, unmodified, running on native views. Nothing moves into this repository and <code>ng build</code> keeps working."
  code={`npx @angular/cli@latest new my-app && cd my-app
an init
an add ios
an ios`}
  notes={[
    { command: 'an init', what: 'Dependencies, tsconfig and an entry point. It touches neither src/main.ts nor angular.json.' },
    { command: 'an add ios', what: 'Writes ios/Info.plist, yours from then on.' },
    { command: 'an ios', what: 'Builds, assembles the .app without Xcode and launches it in the simulator.' }
  ]}
/>

<Closing
  title="Write Angular."
  accent="Ship native."
  body="An ordinary ng new project, four commands, and it is running on real UIKit and real Material views."
  action="Quick start"
  href="/guide/quick-start/"
/>
