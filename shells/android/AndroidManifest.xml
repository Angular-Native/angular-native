<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.angularnative">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="36" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Sin barra de acción: la pantalla la dibuja la app entera, igual que en
      iOS, y el layout ya reserva el hueco de la barra de estado.

      El tema es el de Material 3, que viene con la librería de Material: la
      plataforma no lo trae. Con `DeviceDefault` los controles salían con el
      aspecto que el fabricante mantiene al día, que no es lo mismo: los
      componentes de Material 3 —la barra de navegación, los botones— no están
      en la plataforma y necesitan su tema para dibujarse bien.
    -->
    <application
        android:label="AngularNative"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
