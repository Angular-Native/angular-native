target/
# Sin la barra: en un worktree lo normal es enlazarlo al del repo principal en
# vez de instalarlo otra vez, y un enlace no es un directorio para git.
node_modules
dist/
.DS_Store
*.xcuserdatad/
build/

# Emulador y artefactos de Android
*.apk

# Las dependencias de Android: las baja `scripts/fetch-android-deps.py` y son
# 11 MB de binarios de terceros que no tienen por qué vivir en el repo.
vendor/android/
