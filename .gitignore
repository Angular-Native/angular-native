target/
# Sin la barra para que valga a cualquier profundidad: hay uno en la raíz y
# otro en `docs-site`, que trae su propio Astro y no comparte nada con el
# framework.
node_modules
.astro/
dist/
.DS_Store
*.xcuserdatad/
build/

# Emulador y artefactos de Android
*.apk

# Las dependencias de Android: las baja `scripts/fetch-android-deps.py` y son
# 11 MB de binarios de terceros que no tienen por qué vivir en el repo.
vendor/android/
